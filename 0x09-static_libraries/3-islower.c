#include "main.h"

/**
 * _islower - it prints 1 or 0 depending on input
 * @c: first parameter
 *
 * Description: prints all small letters
 * Return: Always(0).
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
